export const ADD_THUMBNAIL = "ADD_THUMBNAIL";

export const addThumbnail = (thumbnail) => ({
  type: ADD_THUMBNAIL,
  thumbnail,
});

export const deleteThumbnail = (thumbnail) => ({
  type: ADD_THUMBNAIL,
  thumbnail,
});

export const updateThumbnail = (thumbnail) => ({
  type: ADD_THUMBNAIL,
  thumbnail,
});

export const moveThumbnailToOtherWorksapce = (thumbnail) => ({
  type: ADD_THUMBNAIL,
  thumbnail,
});

export const sortThumbnailsBy = (by) => ({
  type: ADD_THUMBNAIL,
  by,
});

export const copyThumbnail = (id) => ({
  type: ADD_THUMBNAIL,
  id,
});
